const [a, b, c, d] = [3, 7, 15, 9];

const soma = a + b;
const subtracao = b - a;
const multiplicacao = c * a;
const divisao = -d / a;
const modulo = (-c + 1) % 2;

console.log(soma, subtracao, multiplicacao, divisao, modulo);